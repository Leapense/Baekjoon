#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    long long M, V, n, K;
    cin >> M >> V;
    cin >> n;
    cin >> K;
    
    long long total = 40 + 2 * n;
    cout << total << "\n";
    
    return 0;
}